1. Create ceasar-cipher formular, receive character index & step
2. Receive user message and split message into array "plain"
3. Compare each elements in "plain" to the alphabets up and down
4. If matches, then find index with cipher_formular and push to array "cipher"
5. If not matches, push to "cipher"
6. Print out the result
